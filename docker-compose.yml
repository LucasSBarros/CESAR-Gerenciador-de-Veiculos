services:
  javaapp:
    build: ./java-app/
    depends_on:
      - db

  db:
    build: ./db/
    restart: always
    environment:
      POSTGRES_DATABASE: 'db'
      POSTGRES_USERNAME: 'postgres'
      POSTGRES_PASSWORD: '*'
    ports:
      - '5432:5432'